---
description: Fetch a URL or local file and store it in the materials bank
argument-hint: <url-or-path> [--name "label"]
allowed-tools: Bash
---

Run the following command exactly as written, substituting $ARGUMENTS:

```bash
node scripts/tools/fetch_materials.js $ARGUMENTS
```

Report the saved file path, title, type, and character count from the output.
